# Global Static Constants

Holds hardcoded variables and lists that do not require environment configuration variables.

## Core Files

- `index.ts`: Outlines application limits (e.g. layout sizes, project thresholds, maximum upload boundaries).
- `billing.ts`: Hardcoded product feature lists for Pricing cards mapping.
