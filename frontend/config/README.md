# Application Configurations

Configuration files capturing navigation links, API endpoints, feature flags, and deployment limits.

## Core Files

- `site.ts`: Houses branding values, title headers, and meta descriptions.
- `dashboard.ts`: Outlines page-routing nodes, icons, and labels.
