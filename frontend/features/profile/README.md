# Feature: Profile

Handles individual user profile updates, display preferences, and password change hooks.

## File Structure

- `components/`: ProfileForm, AvatarUpload.
- `hooks/`: `useUserProfile`.
- `services/`: Sync user table updates.
- `types/`: Profile schemas.
- `utils/`: Display name initials parser.
