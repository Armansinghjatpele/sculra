# Feature: Projects

This module handles project configurations, target domains under test, and repository integrations.

## File Structure

- `components/`: ProjectCardGrid, CreateProjectModal, CredentialForm.
- `hooks/`: `useProjectsList`, `useProjectDetails`.
- `services/`: Project database access rules.
- `types/`: Project credentials and setting shapes.
- `utils/`: URL format validation.
