# Global Custom React Hooks

This directory houses global React hooks used across multiple features or components.

## Examples

- `useMediaQuery.ts`: For listening to screen viewport breakpoints.
- `useLocalStorage.ts`: For synchronizing settings in client-side local storage.
- `useDebounce.ts`: For input throttling.
