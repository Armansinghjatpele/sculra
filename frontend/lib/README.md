# External Library Integrations

This directory configures instances of third-party libraries.

## Configured Clients

- `supabase.ts`: Client instance configurations.
- `posthog.ts`: Client config tracking scripts.
- `sentry.ts`: Global application error reporting setup.
- `utils.ts`: Standard library helpers (e.g. `cn` class merger wrapper).
