# Frontend TypeScript Types

This directory defines Typescript contracts specific to components and client layout states.

## Files

- `theme.ts`: Defining Theme modes (`light`, `dark`, `system`).
- `navigation.ts`: Defining sidebar and menu routing shapes.
- `index.d.ts`: Catch-all type definitions for custom window objects (e.g. analytics scripts).
