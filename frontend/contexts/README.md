# React Contexts

Provides global UI state contexts mapping overlays, search bars, and workspace scopes.

## Core Files

- `CommandMenuContext.tsx`: Tracks keyboard shortcuts triggering search overlays.
- `WorkspaceContext.tsx`: Manages active tenant organization selector state.
