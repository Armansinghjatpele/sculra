# Global Styles and CSS Variables

This directory houses the design system tokens, Tailwind directives, and glassmorphism styling utilities.

## Core Files

- `design-system.css`: Core CSS variables (backgrounds, borders, elevations).
- `globals.css`: Primary tailwind directive index linking fonts and scrollbar resets.
