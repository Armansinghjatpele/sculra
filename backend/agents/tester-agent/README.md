# AI Tester Agent

Discovers pages, fills forms, clicks buttons, and executes standard UI workflows to discover bugs and exceptions.

## Action Matrix

1. Intercept active DOM nodes.
2. Select target nodes (e.g. login form submission button).
3. Synthesize mock inputs (valid/invalid combinations).
4. Assert post-condition state and capture runtime failures.
