# Sculra Maintenance & Seed Scripts

This folder contains command-line TypeScript scripts used for dev system setup, database provisioning, migration testing, and cache purges.

## Scripts

- `setup-db.ts`: Triggers schema reset or updates against local/staging dockerized database instances.

