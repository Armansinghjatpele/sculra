# Enforce LF line endings for all text files to ensure compatibility between Linux/macOS and Windows
* text=auto eol=lf

# Force binary files to retain original format
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.ico filter=lfs diff=lfs merge=lfs -text
*.pdf -text
